%tr
  %td= term.title
  %td= term.definition
  %td
    - unless current_user.learning?(term)
      = form_with method: :patch, url: learn_competence_term_path(competence, term) do |form|
        = form.submit t('helpers.actions.learn'), id: "learn-btn-#{term.id}", class: "btn__learn"